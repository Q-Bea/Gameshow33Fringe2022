import { Collection } from "mongodb";

export default class MongoPlayerDataFunctions {
    /**
     * @type {Collection}
     */
    collection

    constructor(collection) {
        this.collection = collection;
    }

    addPlayers(playerNameObj) {
        
    }
}